{
  "name": "osakarovka-taxi-bot",
  "version": "1.0.0",
  "description": "WhatsApp taxi dispatcher — еОсакаровка Сервис",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "setup": "node src/db/setup.js",
    "test": "cross-env NODE_OPTIONS=--max-old-space-size=512 jest --runInBand",
    "test:coverage": "cross-env NODE_OPTIONS=--max-old-space-size=512 jest --runInBand --coverage"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "axios": "^1.6.0",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "groq-sdk": "^1.2.1",
    "node-cron": "^3.0.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.4.2",
    "nodemon": "^3.0.3"
  }
}
